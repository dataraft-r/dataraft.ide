% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bridge.R
\name{ide_emit}
\alias{ide_emit}
\title{Write one bounded metadata response to a private JSON file}
\usage{
ide_emit(
  kind,
  response_path,
  request_id,
  context = ide_context(),
  handle = NULL,
  selection = "workspace",
  limit = 100L,
  row_limit = 100L,
  file_path = NULL
)
}
\arguments{
\item{kind}{Metadata operation, or explicitly requested view/trial action.}

\item{response_path}{Unused absolute JSON file path in an existing private directory.}

\item{request_id}{Correlation identifier containing letters, digits, hyphens or underscores.}

\item{context}{Context from \code{\link[=ide_context]{ide_context()}}.}

\item{handle}{Optional opaque product or result handle.}

\item{selection}{Workspace or lake context handle.}

\item{limit}{Maximum metadata records, at most 500.}

\item{row_limit}{Maximum viewed or sample-checked rows, at most 1000.}

\item{file_path}{Explicit local ODCS YAML file for contract operations only.}
}
\value{
Invisibly, the envelope written to the file.
}
\description{
The caller creates a private response directory and supplies an unused file
name. A sibling temporary file is renamed atomically after serialization.
Existing files and symbolic links are never overwritten. Use one outstanding
request per destination. Responses are limited to one MiB; errors contain
fixed redacted messages. The file transport is for a trusted local IDE, not
an authentication boundary or a remote network service.
}
\examples{
path <- tempfile(fileext = ".json")
ide_emit("contexts", path, "example")
jsonlite::fromJSON(path)$contract
unlink(path)
}
