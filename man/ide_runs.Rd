% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata.R
\name{ide_runs}
\alias{ide_runs}
\alias{ide_quality}
\alias{ide_incidents}
\alias{ide_releases}
\alias{ide_freshness}
\alias{ide_reports}
\title{Read execution metadata for an IDE}
\usage{
ide_runs(
  context = ide_context(),
  selection = "workspace",
  handle = NULL,
  limit = 100L
)

ide_quality(
  context = ide_context(),
  selection = "workspace",
  handle = NULL,
  limit = 100L
)

ide_incidents(
  context = ide_context(),
  selection = "workspace",
  handle = NULL,
  limit = 100L
)

ide_releases(
  context = ide_context(),
  selection = "workspace",
  handle = NULL,
  limit = 100L
)

ide_freshness(
  context = ide_context(),
  selection = "workspace",
  handle = NULL,
  limit = 100L
)

ide_reports(
  context = ide_context(),
  selection = "workspace",
  handle = NULL,
  limit = 100L
)
}
\arguments{
\item{context}{Context from \code{\link[=ide_context]{ide_context()}}.}

\item{selection}{Workspace or lake context handle.}

\item{handle}{Optional product or result handle.}

\item{limit}{Maximum records, from 1 to 500.}
}
\value{
Bounded records containing metadata only.
}
\description{
Lake functions read existing registry metadata; they do not connect, run
products, collect product rows or publish outputs. Workspace functions read
already retained run results. Descriptions, SQL, diagnostic messages and
failure rows are excluded from execution records.
}
